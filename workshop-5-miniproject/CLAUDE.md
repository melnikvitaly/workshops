# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Read **[`AGENTS.md`](AGENTS.md)** — it holds the guidance for this project
(repo shape, commands, the firmware and camera architectures, and the
invariants that span several files). It is kept toolchain-agnostic so every
agent reads the same notes, and this file exists only to point at it.

[`README.md`](README.md) is the project document: task, parts, wiring, the
`make` targets, safety, and TODO. `AGENTS.md` deliberately does not repeat it.
